---
date: 2026-6-19
category: fun
title: Reading log
---

`reading log`，`log` 是什么意思？对数。把 `e` 和 `i` 提出来，这不就是 `radng logei` 吗，那以 `e` 为底的对数不就是 `ln` 吗。那 `ln(i)=ln(e^(iπ/2+2kπi))` ，那不就等于 `radng i(π/2+2kπ)` 吗
话又说回来，`rad` 的意思就是弧度，`π/2+2kπ rad = 90°`,原式 `=ing 90°`，`°=degree`，`90>1`，加复数
`90ingdegrees`，那就是 `regret*90ingdes/rt`，`regret v. 后悔`，`houhui*90ingdes/rt`，`2r=d`，原式 `=houhui*180inge ds/dt=houhui*180ingev`
注意到 `i^2=-1`，原式 `=-180hhuuengov`，注意到乘号简写为点，`-180huhune.gov` 是个网址，至于呼 `hu` 呼 `hu` 呢 `ne` 是啥，吾怎知？